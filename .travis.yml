language: python
python:
  - "3.6"
services:
  - docker

before_script: pip install docker-compose

script:
  - docker-compose run --rm web bash -c "while !</dev/tcp/db/5432; do sleep 1; done; python manage.py test --settings=app.settings.development && flake8"